============
Installation
============

At the command line::

    $ easy_install django-getpaid-przelewy24

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-getpaid-przelewy24
    $ pip install django-getpaid-przelewy24
